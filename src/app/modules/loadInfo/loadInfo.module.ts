import { BrowserModule } from '@angular/platform-browser';
import { NgModule } from '@angular/core';

import { LoadInfoComponent } from './loadInfo.component';

@NgModule({
  declarations: [
    LoadInfoComponent
  ],
  imports: [
    BrowserModule
  ],
  providers: [],
  bootstrap: [LoadInfoComponent]
})
export class AppLoadInfo { }
